---
import { Tv } from 'lucide-react';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const { 
  title,
  description = "Актуальные промо коды для российских стриминговых сервисов 2024 года. Экономьте на подписках с нашими промокодами!",
  ogImage = "https://promokodkino.ru/og-image.jpg"
} = Astro.props;

// Fix: Use a safe fallback for canonical URL
const siteUrl = import.meta.env.SITE ?? 'https://promokodkino.ru';
const canonicalURL = new URL(Astro.url.pathname, siteUrl);
---

<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    
    <!-- SEO Meta Tags -->
    <title>{title} | Промо коды для стриминговых сервисов</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={ogImage} />
    
    <!-- Structured Data -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "Промо коды для стриминговых сервисов",
        "description": "Актуальные промо коды для российских стриминговых сервисов",
        "url": siteUrl
      }
    </script>
  </head>
  <body class="min-h-screen bg-[#121212] text-white flex flex-col">
    <header class="bg-[#1e1e1e] py-6 px-4 shadow-lg">
      <div class="max-w-7xl mx-auto">
        <a href="/" class="inline-block hover:opacity-80 transition-opacity">
          <h1 class="text-3xl font-bold mb-4 flex items-center">
            <Tv className="w-8 h-8 mr-3 text-blue-400" />
            Промо коды для стриминговых сервисов России
          </h1>
        </a>
      </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 py-8 flex-grow w-full">
      <slot />
    </main>

    <footer class="bg-[#1e1e1e] py-8 px-4 mt-auto">
      <div class="max-w-7xl mx-auto">
        <div class="grid md:grid-cols-2 gap-8 mb-8">
          <div>
            <h2 class="text-2xl font-bold text-white mb-4">Нашли новый промо код?</h2>
            <p class="text-gray-400">
              Помогите другим пользователям сэкономить! Поделитесь промо кодами, которые вы нашли, 
              и они появятся на сайте после проверки. Вместе мы делаем стриминг доступнее!
            </p>
          </div>
          <div>
            <form class="space-y-4">
              <div>
                <label for="serviceName" class="block text-sm font-medium text-gray-300 mb-1">
                  Сервис
                </label>
                <input
                  type="text"
                  id="serviceName"
                  class="w-full bg-[#2a2a2a] text-white px-4 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                  required
                  placeholder="Название стримингового сервиса"
                />
              </div>
              <div>
                <label for="promoCode" class="block text-sm font-medium text-gray-300 mb-1">
                  Промо код
                </label>
                <input
                  type="text"
                  id="promoCode"
                  class="w-full bg-[#2a2a2a] text-white px-4 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                  required
                  placeholder="Введите промо код"
                />
              </div>
              <div>
                <label for="discount" class="block text-sm font-medium text-gray-300 mb-1">
                  Описание скидки
                </label>
                <input
                  type="text"
                  id="discount"
                  class="w-full bg-[#2a2a2a] text-white px-4 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                  required
                  placeholder="Например: 30% на первые 3 месяца"
                />
              </div>
              <div>
                <label for="validUntil" class="block text-sm font-medium text-gray-300 mb-1">
                  Действует до
                </label>
                <input
                  type="date"
                  id="validUntil"
                  class="w-full bg-[#2a2a2a] text-white px-4 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                  required
                />
              </div>
              <div>
                <label for="terms" class="block text-sm font-medium text-gray-300 mb-1">
                  Условия использования
                </label>
                <input
                  type="text"
                  id="terms"
                  class="w-full bg-[#2a2a2a] text-white px-4 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                  placeholder="Например: Только для новых пользователей"
                />
              </div>
              <button
                type="submit"
                class="w-full bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition-colors flex items-center justify-center gap-2"
              >
                Отправить промо код
              </button>
            </form>
          </div>
        </div>
        <div class="text-center text-gray-400 pt-8 border-t border-gray-700">
          <p>© 2024 Каталог промо кодов для стриминговых сервисов. Все права защищены.</p>
        </div>
      </div>
    </footer>
  </body>
</html>